meta:
    workdir: .
    watches:
        - "meta-cli/src/**/*.rs"
    shell: "cargo run -p meta -- --help"
    depends_on:
        - libs

examples:
    workdir: .
    watches:
        - "examples/metatype.yaml"
    shell: "cargo run -p meta -- -C examples dev"
    envs:
        VIRTUAL_ENV: $PWD/examples/.venv
        PATH: $PWD/examples/.venv/bin:$PATH
    depends_on:
        - meta

system_graphs:
    workdir: typegate
    watches: src/typegraphs/*.py
    shell: "./serialize-tgs.sh"
    depends_on:
        - meta

typegate1:
    workdir: typegate
    watches: "src/**/*.ts"
    envs:
        TG_PORT: "7891"
        PACKAGED: "false"
        REQUEST_LOG: "tg1"
    shell: "./run.sh"
    depends_on:
        - clean_deno_bindgen_cache
        - typegate_native

typegate2:
    workdir: typegate
    watches: "src/**/*.ts"
    envs:
        TG_PORT: "7892"
        PACKAGED: "false"
        REQUEST_LOG: "tg2"
    shell: "./run.sh"
    depends_on:
        - clean_deno_bindgen_cache
        - typegate_native

libs:
    workdir: libs
    watches: "**/*.rs"
    shell: "cargo build -p common"

clean_deno_bindgen_cache:
    shell: rm -rf $(deno info --json | jq -r .denoDir)/plug/file bindings.json
    depends_on:
        - typegate_native

typegate_native:
    watches:
        - "typegate/native/src/**/*.rs"
    shell: "deno_bindgen -- -p native"
    depends_on:
        - libs

website_deps:
    workdir: website
    watches: "package.json"
    shell: "pnpm install"

website:
    workdir: website
    shell: "pnpm start --no-open"
    depends_on:
        - website_deps

website_meta:
    workdir: .
    watches:
        - "meta-cli/src/**/*.rs"
        - "website/**/*.py"
    shell: "cargo run -p meta -- -C website dev -p password --port 5001"
    envs:
        VIRTUAL_ENV: $PWD/examples/.venv
        PATH: $PWD/examples/.venv/bin:$PATH
    depends_on:
        - libs
        - typegate_native
