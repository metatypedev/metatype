
To upgrade the _Python_ SDK of the typegraph package, you can run either of the following commands based on the dependency manager you are using.

- pip
```shell
pip3 install --upgrade typegraph
```

- poetry
```shell
poetry add typegraph@latest
```
