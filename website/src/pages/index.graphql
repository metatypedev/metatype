# command/ctrl + enter to run
query A {
  stargazers {
    login
  }
}

query B {
  stargazers {
    login
    # composition
    user {
      name
    }
  }
}

mutation C($message: String!) {
  send_feedback(
    data: {
      email: "" # fill me
      message: $message
    }
  ) {
    id
    message
  }
}

query D {
  list_feedback {
    email # cannot be access, delete me
    message
  }
}
