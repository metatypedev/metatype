---
sidebar_position: 1
---

import PaginatorNavLink from "@theme/PaginatorNavLink";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import DocButton from "@site/src/components/DocButton";
import Newsletter from "@site/src/components/Newsletter";
import Metatype from "../shared/metatype.mdx";

# Getting started

<Metatype />

## Introduction

In this tutorial, you will write your first typegraph and deploy it on the typegate using the Meta CLI. You will design some API blocks for a chat-based app where users can discuss together and share products.

:::info Looking first for explanation?
For concepts, head over to the [overview](/docs/concepts/overview) to understand the ecosystem.
:::

Running the ecosystem requires Python >= 3.8 which should be available on most the recent OS by default.

<Tabs>
  <TabItem value="linux" label="Debian-based Linux" default>

```bash
sudo apt-get update
sudo apt-get install python3 python3-pip
```

  </TabItem>
  <TabItem value="macos" label="MacOS">

```bash
brew install python3
```

  </TabItem>
  <TabItem value="windows" label="Windows">

Download the [installer](https://www.python.org/downloads/windows/) from the official Python website and follow the steps.

  </TabItem>
</Tabs>

## Quickstart with Metatype Cloud

This is the easiest way to get started, yet it's still in private beta. You can sign-up for the waiting list below.

<Newsletter />

## Quickstart with Docker

### 1. `meta` CLI

You can download the binary executable from
[releases](https://github.com/metatypedev/metatype/releases/), make it
executable and add it to your `$PATH` or use
[eget](https://github.com/zyedidia/eget) to automate that step.

```bash
eget metatypedev/metatype --to $HOME/.local/bin

meta --help
meta upgrade
```

### 2. `typegraph` package

```bash
python3 -m venv .venv
source .venv/bin/activate
pip3 install typegraph --upgrade

python3 -c 'import typegraph; print(typegraph.version)'
```

### 3. `typegate` node

```bash
curl https://raw.githubusercontent.com/metatypedev/metatype/main/examples/docker-compose.yml -o docker-compose.yml
docker compose up -d

curl -X POST http://localhost:7890/typegate \
    -H 'Authorization: Basic YWRtaW46cGFzc3dvcmQ=' \
    --data-binary '{"query":"query list { typegraphs { name  }}"}'
```

### Alternatively, install from source

```
pip3 install --upgrade git+https://github.com/metatypedev/metatype#subdirectory=typegraph
cargo install --force meta-cli --git https://github.com/metatypedev/metatype
```
