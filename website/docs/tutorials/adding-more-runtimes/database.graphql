mutation A {
  create_message(
    data: {
      # id is autogenerated
      title: "Hello World"
      body: "This is a message"
    }
  ) {
    id
  }
}

query B {
  list_messages(where: { title: { equals: "Hello World" } }) {
    id
    body
  }
}
