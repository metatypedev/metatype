---
sidebar_position: 50
---

import TGExample from "@site/src/components/TGExample";
import CodeBlock from "@theme-original/CodeBlock";

# Write REST endpoints

The materializers we expose from our typegraphs are currently provided with a GraphQl API by default.
If we have need for a REST API, we can easily achieve that by the `rest` function which takes GraphQl queries and provides RESTly endpoints for them. 

<CodeBlock language="python">{require("../../../../examples/typegraphs/example_rest.py").content}</CodeBlock>
